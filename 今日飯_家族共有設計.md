# 今日飯 家族共有 設計（ドラフト）

サポーター（¥150/月）特典の「家族共有」。夫婦など同一世帯の複数端末で、冷蔵庫・献立・家族情報を共有する。

## 0. 大原則との整合
- 既存原則：**健康情報（アレルギー・妊娠/授乳）は端末内のみ・クラウドに置かない**。
- 家族共有は「同一世帯の複数端末で同じデータを見る」＝**サーバ同期が必須**。
- → 家族共有を**有効化した世帯に限り**、必要データをクラウド（世帯内のみ閲覧可）に置く。
  非共有ユーザーは**従来どおり完全ローカル**（既定）。AI呼び出しは従来どおり安全フラグのみ送信・保存なし。

## 1. 共有する範囲【確定：フル世帯】
| データ | 共有 | 機微度 |
|---|---|---|
| 冷蔵庫の在庫・まな板・残量(卵/米) | ◯ | 低 |
| 食育履歴・栄養 | ◯ | 低 |
| 家族構成・アレルギー・体の状態 | ◯（**確定**） | **高（健康情報）** |

- 家族構成＋アレルギーも共有する。両親が同じ「食べる人」「安全条件」を見られ、提案の安全性が世帯で一致。
- 健康情報はクラウドに乗るが、**世帯内のみRLSで保護＋有効化前に同意画面**（§6）。非共有ユーザーは従来どおり完全ローカル。

## 2. 認証【確定：Apple/Google サインイン】
- 非共有ユーザー：ログイン不要（従来の完全ローカル）。
- 家族共有を使う時だけ **Sign in with Apple / Google** が必要（複数端末で同一IDを跨ぐため）。機種変更・復元に強く、Appleの自社ログイン必須要件にも合致。
- 実装：Supabase Auth の Apple/Google プロバイダ。

## 3. アーキテクチャ（推奨）
- 基盤：**Supabase**（既にEdge Functionで使用）。Postgres ＋ Row Level Security ＋ Realtime。
- 世帯モデル：
  - `households`(id, name, owner_user_id, invite_code, plan)
  - `household_members`(household_id, user_id, role, joined_at)
  - `eaters`(household_id, label, kind, child_age, conditions[], allergies jsonb) ← 案A時のみ
  - `fridge_items`(household_id, name, amount, unit, qty, qty_max, on_board, updated_at, updated_by)
  - `meal_history`(household_id, dinner_date, title, categories[], cooked_at, fatigue)
- RLS：`household_members` に属するユーザーだけが自世帯の行を読み書き可。
- 同期：Realtime（postgres_changes）で各端末へ反映。**AsyncStorageはオフラインキャッシュ**として継続。
- 競合：行単位 last-write-wins（`updated_at`）。在庫はidキー、残量編集も後勝ちで実用上十分。

## 4. 参加フロー
1. サポーターが「家族を招待」→ 6桁コード（＋QR/共有リンク）発行。
2. 家族が「家族に参加」でコード入力 → `household_members` に追加。
3. 以後、双方の冷蔵庫・献立・（案Aなら家族設定）がリアルタイム共有。

## 5. 課金との関係
- 世帯の作成・参加は**オーナーがサポーターであること**が条件。
- 招待された家族は、**自分が未課金でも**世帯データを共有できる（オーナーの購読で賄う）。
  - ※Appleの「ファミリー共有」で購読自体を分け合う方式とは別。本機能はアプリ側の世帯共有。
- オーナーが解約：同期を停止し、各端末に**最後のスナップショットをローカル保持**（読み書きはローカルのみ）。再加入で同期再開。

## 6. プライバシー対応（必須）
- 家族共有の**有効化前に同意画面**：「共有する情報＝家族構成・アレルギー・在庫・履歴。家族のみ閲覧可。広告/AI学習には使わない」。
- 健康情報はRLSで世帯内に限定・Supabaseの保存時暗号化。AI呼び出しは従来どおり**安全フラグのみ・保存なし・氏名は送らない**。
- プライバシーポリシーに家族共有の項目を追記。

## 7. 実装ステップ（概算）
1. Supabase Auth 有効化（Apple/Google）＋ テーブル＆RLS
2. クライアント：認証・世帯作成/参加UI、同期レイヤ（ローカル⇄Supabase）、Realtime購読
3. 既存ローカル保存（members/fridge/history）を「ローカル or 世帯」を切り替える抽象化
4. 同意画面・解約時フォールバック
5. テスト（2端末・オフライン・競合・解約）

— 規模：**中〜大**。EAS開発ビルド前提（認証・Realtime）。MVPは「在庫＋履歴のみ共有（案B）」で小さく出し、案Aは後追いも可。
