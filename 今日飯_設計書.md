# 今日飯 -限界レシピ提案- 設計書

> 共働き子育て世帯向け。「夕飯を決める意思決定を代行する」アプリ。
> レシピ検索アプリではなく「もう考えたくないから決めてほしい」に応えるアプリ。

最終更新: 2026-06-27

---

## 0. 関連ファイル（成果物）

| ファイル | 内容 |
|---|---|
| `今日飯_設計書.md` | 本書（全体設計の単一の真実） |
| `fallbackRecipes.json` | フォールバック献立30品（AI失敗・オフライン時の保険。安全フラグ＋アレルゲン付き） |
| `ingredientDict.json` | 食材名 → カテゴリ／アレルゲン正規化辞書（栄養集計・アレルゲン照合の土台） |

---

## 1. コンセプトと最上位原則

このアプリの成否は「**ユーザーに何回意思決定させたか**」で決まる。

- **理想は2タップ献立**：疲労度を1タップ → 「決めて」を1タップ → 完了。
- 冷蔵庫・まな板・方向性タグ・食べる人選択は**すべて任意**。何も触らなくても答えが出る。
- 迷わせない／判断させない／最終的に「今日はこれを作ればいい」まで導く。
- 設計判断の基準：**タップ数と思考量を増やすものは却下**。

### ターゲット
- 共働き家庭／保育園児〜小学生の子どもがいる家庭
- 平日18〜20時、保育園帰り・仕事終わりで疲労している状態
- 思考コストを限界まで下げたい人

### デザイン方針
iPhone向け（iPad対応）／白ベース＋ナチュラルグリーン／暖色トーン／情報量を減らす／ボタン大きめ／日本語UI。

---

## 2. 画面構成

```
オンボーディング（初回のみ・3画面・スキップ可）
├─ ① ホーム（疲労度選択）         ← 起動直後
├─ ② メイン（今日の飯を決める）
│     ├─ 疲労度（設定済み・小さく）
│     ├─ 今夜食べる人（チップ・既定で全員オン）
│     ├─ 冷蔵庫（食材カード横スクロール）
│     ├─ まな板（任意・今日使いたい食材の置き場）
│     ├─ 方向性タグ（任意・複数選択・疲労度で自動点灯）
│     └─ 最重要ボタン「決めて」
├─ ③ 提案結果（疲労度で1〜3品）
├─ ④ 食育履歴 / 栄養バランス
├─ ⑤ 冷蔵庫登録（レシート/写真/手入力）
├─ ⑥ 設定（食べる人・サポーター 等）
└─ 課金/応援（コーヒー・サポーター）
```

タブは最小3つ：［今日の飯］［冷蔵庫］［履歴］。

---

## 3. ユーザーフロー

### 最速ルート（限界の人向け）
```
起動 → 疲労度「限界」 → 「何でもいいから決めて」 → 1品提示 → 終わり
```
冷蔵庫が空でも、履歴と限界モードだけで提案を返す（在庫ゼロでも必ず答えを出す）。

### 標準ルート
```
起動 → 疲労度 → （任意）食べる人/まな板/方向性 → 「決めて」 → 提案 → これにする → 履歴に自動記録
```

### 別の案ループ
提案 → 「別の案」→ 次候補（**最大3回**）。4案目は出さず「今日はこの中から」で打ち切る（決断回避の防止＋AIコスト上限）。

---

## 4. 画面仕様

### ① ホーム（疲労度選択）
- 起動直後にこれ1問だけ。選択肢は4つ、操作は1タップ。
- 2×2の大カード（各最低120px高）：**元気 / 普通 / 疲れた / 限界**。アイコン＋ラベル＋行動レベルのサブ（元気=しっかり作れる／限界=もう決めて）。
- 入口コピーは味方トーン（「おかえりなさい。今日はどれくらい疲れてる？」）。
- ここで選んだ疲労度が、方向性タグ自動点灯・提案数・ボタン文言・広告量・工程数まで連動。

### ② メイン（今日の飯を決める）
- 上から `疲労度 → 今夜食べる人 → 冷蔵庫 → まな板 → 方向性 → 決めて`。
- **今夜食べる人**：登録メンバーを横並びアバターで表示、既定で全員オン（`3人分`自動）。タップで増減し、選ばれた人の登録情報から安全条件を再計算（§7）。**毎日全員オンにリセット**。
- 冷蔵庫：食材カード（名前・残量3段階・期限）。期限近は黄バッジで軽い示唆のみ（赤字で攻めない）。期限近い順に自動ソート。
- まな板：冷蔵庫からタップで置いた食材の一覧（緑の囲み）。空でOK。
- 方向性タグ：今日を乗り切る/時短/レンジ併用/包丁なし/洗い物少なめ/栄養優先/子ども完食/冷蔵庫整理/節約。疲労度で初期点灯。
- 「決めて」：下部固定・全幅・最大。疲労度で文言可変（限界→「何でもいいから決めて」）。「在庫が空でも決められます」を添える。
- **限界モードは食べる人チップを1行に畳む**（「家族3人ぶんで決めます」＋小さな`変える›`）。聞かずに黙って正しくやる。安全配慮は効いていることを明示。

### ③ 提案結果（疲労度で提案数が可変）

| 疲労度 | 提案数 | 見せ方 |
|---|---|---|
| 限界 | 1品（固定） | ドーンと1つ。選択肢ゼロ |
| 疲れた | 1品 | 1つ＋「別の案」 |
| 普通 | 1品＋候補2 | おすすめ1つ＋控えめな候補 |
| 元気 | 3品 | 比較OK。**必ず1つに「おすすめ」** |

- 1品表示は：料理写真（プレースホルダ。本番はAI生成 or 素材写真。MVPはアイコン＋色で可）→ 料理名（画面最大文字）→ **3指標（所要時間 / 洗い物 / 子ども取り分け）** → 提案理由（1文）→ 使う食材 → かんたん工程（最大3〜4）。
- ボタン階層：`これにする`（緑・最大）＝決定で履歴へ自動記録／`別の案 残りN`／`材料を見る`。
- 元気の複数表示も**おすすめを視覚的に突出**させ、「迷ったら一番上」で即決できる逃げ道を残す。
- **普通モード**：おすすめ1品をフル表示＋下に「他の候補」2件を控えめに（元気の3列比較とは区別）。

### ③' 材料を見る（安全確認の置き場）
- 提案結果からの遷移先。**重度アレルギー時の最終確認はここ**。
- 食材を **「冷蔵庫にあるもの／買い足すもの」に分離**（買い足し分にはアレルゲン注記：例 めんつゆ→小麦/大豆）。
- アレルギー世帯は先頭に**確認バナー**：「対象食材は含まれません／市販調味料は商品により異なるため、召し上がる前に必ずご確認ください」（安全担保＋保証しない正直さ）。
- 子ども・妊娠中の配慮メモを集約（取り分け・卵の加熱）。分量は出さない（2タップ思想を維持）。「作り始める」で調理へ。

### ④ 食育履歴 / 栄養バランス
- 「管理」でなく「気づき」。細かい栄養素は出さず、食材カテゴリ単位の回数だけ。
- 上部に結論＋次アクション（「今週は魚が不足ぎみ → 次の提案で魚を優先」）、下に根拠の棒グラフ。
- 色は2色（足りている＝緑／不足ぎみ＝黄）。赤は使わず罪悪感を与えない。
- 「これにする」押下で自動追記＆カテゴリ自動タグ付け（手動記録ゼロ）。
- 偏り判定は §8.5 のロジック。記録3メニュー未満の週は判定しない（「まだ記録が少なめ」と表示）。

### ⑤ 冷蔵庫登録（3手段 → 共通ゴール）
- 優先度を見た目の大きさで表現：**レシート撮影（おすすめ・緑塗り）** ＞ 冷蔵庫写真 ＞ 手入力。
- 3手段は入口が違うだけで、共通の「抽出結果を確認・修正 → 冷蔵庫に登録」へ合流。
- 確認画面は**全項目ON初期表示 → 違う物をタップで外す**逆操作。非食材（レジ袋）は自動でグレーアウト。カテゴリ自動付与。
- ボタンは登録件数を明示（「4品を冷蔵庫に登録」）。
- MVPは手入力＋確認画面を先に作り、OCR/写真認識は後から同じ確認基盤に差し込む。

### ⑥ オンボーディング
- 機能でなく「約束」を最初に：「夕飯、もう考えない。レシピを探すアプリじゃありません」。3ページ・スキップ可・最小文章。

### 家族共有（プレミアム）
- 単なる共有でなく「役割の同期」が主役：買い物担当（外出中）と料理担当の状態が見える／買い物中に在庫確認／担当引き継ぎ。
- リアルタイム同期前提（Supabase Realtime）。同時登録の重複解決ルールは要定義。

---

## 4.5 当日の状態管理と在庫消費

### 決定後の状態（TodayDecision）
- 決定したら当日は「今日はこれ」を保持。再起動で疲労度選択に戻さない（意思決定コストを再発生させない）。
- デイ境界：**翌朝5時まで同日**（深夜調理も当日扱い）。5時を越えたら通常ホームへ。
- 起動時分岐：今日の TodayDecision があり `status≠done` →「今日はこれ：◯◯」画面（作り始める／材料を見る／やっぱり別のにする）。`done` or 無し → 通常ホーム（疲労度選択）。
- 「やっぱり別のにする」は許可（罰しない）。当日の履歴を**上書き**（二重カウントしない）。
- 家族共有：配偶者の決定を同期表示（「妻が決めました：◯◯」）。

### 在庫消費（低摩擦・限界では出さない）
- 「作り終わった」時に**1タップ**：使った `fromFridge` 食材を全プリチェックで表示 →「減らす」または「あとで」。
- 減量は残量3段階を1段ダウン（enough→low→empty）。グラム管理なし。
- **限界モードでは在庫プロンプトを出さない**（疲れた日に作業ゼロ）。
- 期限切れは自動削除・催促しない（罪悪感を与えない）。

---

## 5. コンポーネント設計

| コンポーネント | 役割 |
|---|---|
| `FatigueCard` | 疲労度カード（4種）。ホームの主役 |
| `EaterChips` | 今夜食べる人。タップで人数増減＋安全再計算 |
| `IngredientCard` | 食材カード（名前・残量バー・期限バッジ） |
| `FridgeShelf` | 冷蔵庫（横スクロール・期限近い順ソート） |
| `CuttingBoard` | まな板（選択食材の置き場・空状態に安心コピー） |
| `DirectionTag` | 方向性チップ（複数選択・疲労度で初期点灯） |
| `DecideButton` | 最重要ボタン（下部固定・文言可変） |
| `RecipeResultCard` | 提案結果（1品 or 複数。おすすめ強調） |
| `ReasonChip` | 提案理由1文 |
| `ReceiptScanner` / `ExtractionReviewList` | レシート撮影＋抽出確認 |
| `NutritionBalance` | 7日カテゴリ別バー（信号2色） |
| `QuietNativeAd` | 静かなネイティブ広告（決定後のみ・限界は非表示） |
| `SupportSheet` | コーヒー/サポーター |
| `EmptyState` | 「在庫が空でも決められます」等の安心コピー |

---

## 6. データ構造

```
User            id, displayName, householdId(任意), plan(free/premium)
Household        id, members[], sharedFridgeId

EaterProfile（永続・§7）
  members: Member[]
Member           id, label, kind(adult/child), childAge?, conditions[](妊娠中/授乳中), allergies[]

Fridge           id, householdId, items: Ingredient[]
Ingredient       id, name, category, amount(enough/low/empty), expiryDate?, source(receipt/photo/manual), addedAt

DecisionInput（一時・「決めて」時に組み立て → AI/フォールバック共通）
  fatigue, headcount, eatersTonight[], safetyProfile,
  cuttingBoard[], fridge[], fridgeEmpty, directionTags[],
  nutritionDeficits[], avoidTitles[], suggestionCount

Recipe（AI出力＝フォールバックと同一スキーマ・§8）
  title, reason, cookTimeMinutes, servings, washUp,
  childFriendly, childNote?, usedIngredients[], missingIngredients[],
  steps[], tags[], nutritionFocus?

TodayDecision    dinnerDate, recipe, status(cooking/done), decidedBy, decidedAt（当日保持・同日再決定は上書き・§4.5）
MealHistory      id, recipeId, title, dinnerDate, cookedAt, fatigueAtCook, categories[]（メニュー単位ユニークのカテゴリ・スナップショット）
NutritionStat    byCategory{魚,肉,卵,大豆,野菜}, deficits[]（§8.5で算出）
```

設計の肝：
- 残量は数値でなく**3段階**（グラム管理はしない）。
- `MealHistory.ingredientCategories` が栄養偏り判定の中心（`ingredientDict.json` で分類）。

---

## 7. 安全設計（最重要・絶対）

「誰が食べる」は永続設定として持ち、**安全は常時オン・タグ任せにしない**。

### 役割分担
- **誰が食べる（永続設定）＝安全フィルタ**：生もの・喉詰まり・アレルギーを除外。忘れても効く。
- **「子ども完食」タグ＝好み**：安全とは別物。

### SafetyProfile 解決（most restrictive wins）
今夜オンのメンバー集合から OR 合成。

| 誰かが該当 | 適用される禁止/配慮 |
|---|---|
| 妊娠中 | 生卵・生魚・生肉・加熱不足を禁止／水銀の多い大型魚を制限／非加熱チーズ・生ハム禁止／アルコール禁止 |
| 授乳中 | アルコール禁止（カフェイン注意） |
| 子ども0歳 | はちみつ・生もの・喉詰まり禁止 |
| 子ども1〜5歳 | 生卵・生魚禁止／喉詰まり食材は小さく切る |
| 子ども6歳〜 | 生ものは家庭判断（注意書き） |
| アレルギー（全員合算） | 該当食材を含む献立を完全除外 |

出力フラグ：`allergens[], noRawEgg, noRawFish, noRawMeat, noHoney, noHighMercuryFish, noUnpasteurized, noAlcohol, chokingCut, hasChild, hasPregnant, hasInfant, rawCautionForChild`。

しきい値：`INFANT_MAX_AGE=0 / TODDLER_MAX_AGE=5 / CHOKING_MAX_AGE=5 / SCHOOL_MIN_AGE=6`。

### 三重の安全
1. **AI**：システムプロンプトで safetyProfile を最優先に遵守＋自己検算。
2. **クライアント検算**：受信後にアレルゲン/生もの違反を再チェック → 違反なら破棄してフォールバックへ。
3. **フォールバック**：内蔵リストも同じ safetyProfile でフィルタ。

### 正直な但し書き
アレルギーは誤りが致命的になり得るため「アプリはリスクを減らすが保証しない」を明示。重度アレルギー時は提案結果に**材料一覧＋「召し上がる前に材料を必ずご確認ください」**を常時表示。未知食材（辞書 matched:false）は安全と断定せず要確認に倒す。

---

## 8. AI提案エンジン

- **モデル**：`claude-haiku-4-5`（この用途に十分・最安。質を上げたい時は `claude-sonnet-4-6`）。
- **構造化出力**（JSONスキーマで型保証）／**思考なし**（即決・低レイテンシ）／**システムプロンプトはプロンプトキャッシュ**。

### 入力（DecisionInput を JSON で渡す）
疲労度・人数・eatersTonight・**safetyProfile（クライアントで解決済み）**・まな板・在庫・fridgeEmpty・方向性タグ・nutritionDeficits・avoidTitles・suggestionCount。

### システムプロンプト（要旨）
優先順位：**1.安全（絶対） → 2.制約 → 3.好み・栄養**。
- 安全：allergens厳守／noRaw*・noHoney・chokingCut・水銀・非加熱を遵守／作れない時は買い足し前提の安全な定番。
- 制約：1品（複数時は suggestionCount 件・先頭おすすめ・限界/疲れたは必ず1件）／疲労度で工程数（限界・疲れた=最大3、普通・元気=最大4）／headcount人数分／子ども取り分け配慮／まな板優先／在庫空でも返す／avoidTitles除外。
- 出力：reasonは1文／スキーマ厳守／自己検算。

### 出力スキーマ
`{ recipes: Recipe[] }`。Recipe = `title, reason, cookTimeMinutes, servings, washUp, childFriendly, childNote?, usedIngredients[], missingIngredients[], steps[], tags[], nutritionFocus?`。件数はプロンプト指示＋クライアントで丸める。

### 提案数（疲労度連動）
限界=1 / 疲れた=1 / 普通=1(+候補2) / 元気=3。元気でも必ず「おすすめ」1つ。

### フォールバック（`fallbackRecipes.json`）
API失敗・タイムアウト・オフライン・在庫空で**必ず1品出す**。30品・各品に `safety` フラグと `allergens` タグ。選択ロジック：
```
1. avoidTitles と lastId を除外
2. 安全フィルタ（eaterに応じ rawEgg/rawFish/honey/allergens を除外）★絶対に緩めない
3. 疲労度フィルタ（足りなければ疲労度のみ緩める）
4. deficits × categories を優先
5. ランダム選択 → lastId 保存
複数要求時は安全通過後にN件抽出（重複なし・先頭をdeficits優先）
```

### コスト目安
1夕飯 約1円（Haiku＋キャッシュ）。元気の複数提案で約2〜3円。月20回で**1世帯あたり約20〜25円**。

---

## 8.5 栄養偏り判定ロジック

直近7日の `MealHistory` から `nutritionDeficits` を算出（`ingredientDict` の classify を利用）。

- **履歴の保存**：「これにする」時に `usedIngredients` を classify し、メニュー単位でユニーク化したカテゴリ集合 `categories` をスナップショット保存（辞書変更に影響されない）。
- **対象カテゴリ**：魚 / 肉 / 卵 / 大豆 / 野菜（主食・乳・調味料・その他は無視）。
- **アルゴリズム**：
  1. 直近7日のメニューを抽出。記録3未満なら判定しない（`enough:false`）。
  2. カテゴリごとの**出現メニュー数**を数える（1メニュー1カウント）。
  3. 週あたり目標 `{魚:2, 野菜:4, 大豆:2, 肉:2, 卵:2}` を、記録数で `scale = min(件数,7)/7` してスケール。
  4. `presence < ceil(target*scale)` を不足候補に。不足度（needed−count）の大きい順、同点は優先順位 `魚>野菜>大豆>肉>卵`。
  5. **上位2件**を `deficits` として返す（1皿で5項目満たそうとさせない）。
- **接続**：`deficits` を DecisionInput へ（AI/フォールバック共通）／`counts` を履歴の棒グラフへ／`topDeficit` を見出しへ。出力の `nutritionFocus`（単一）＝AIが実際に応えた1カテゴリ。
- **方針**：卵などの超過はペナルティにしない（罪悪感を与えない）。閾値は定数で外出しし運用調整。
- **任意拡張**：`lastMainCategory`（前夜の主カテゴリ）を渡し「可能なら違う主菜に」＝偏りとは別の“変化”軸。

---

## 9. 食材正規化（`ingredientDict.json`）

レシート/写真/手入力の表記ゆれを吸収し、**栄養集計**と**アレルゲン照合**の土台にする。
- 正規化：NFKC・空白除去・量/単位・レシートノイズ除去。
- 照合：完全一致 → 部分一致（最長キー優先）→ 未知。
- 結果：`{canonical, category(肉/魚/卵/大豆/野菜/主食/乳/調味料/その他), allergens[], matched}`。
- 隠れアレルゲンも収録（めんつゆ→小麦/大豆/さば、キムチ→えび、ちくわ→小麦/大豆/卵/さば 等）。
- 未知語は category=その他・allergens=[]・matched=false（安全側で要確認）。
- 将来：OCRで未当たりが多い時のみClaudeで正規化→辞書に追記キャッシュ。

---

## 10. 収益モデル

利益最優先ではなく「広告で多少回収＋お布施（コーヒー）」。原価が安いので成立する。

### 原価（再掲）
変動費＝AI約20〜25円/世帯/月（Haiku＋キャッシュ）。固定費＝Apple約1,250円/月＋小規模はSupabase無料。OCR/画像認識は端末内処理で0円。

### 広告（割り込みなし・決定後のみ・疲労度連動）
意思決定フローは聖域（広告ゼロ）。全画面割り込み・強制視聴は使わない。**ネイティブ広告のみ**（記事風・低コントラスト・閉じられる）。

| 疲労度 | 広告 |
|---|---|
| 限界 | 0（「今日は限界モードなので広告は出しません」に置換） |
| 疲れた | 静かなネイティブ1枚 |
| 普通 | 1枚 |
| 元気 | 調理が長いので最大2枚（レシピ閲覧中にもう1枚まで） |

「広告を見て開発者を応援（任意）」も用意（強制なし）。

**段階的な広告レバー（将来検討）**：まず¥150サブスク＋ネイティブ広告で数か月運用し、収益が不足する場合のみ次を検討する。**元気/普通モードに限り**、操作の“合間”（提案生成中・調理画面など、意思決定の瞬間を妨げない所）に数秒の全画面広告を出す。**限界/疲れたモードには絶対に出さない**（「疲れてるほど静か」の原則は不変）。導入可否は運用データを見て再判断。

### 応援（コーヒー＆サポーター）
- **開発者にコーヒーをおごる**（単発IAP）：¥100 ちょい足し / **¥300 ふつうの1杯（推奨）** / ¥500 ごほうび。
- **サポーター**（自動更新サブスク **¥150/月**）：広告オフ＋家族共有。
- コピーは押し売りしない（「広告だけで十分回っています。余裕のある日だけ。」「しなくても全機能使えます」）。
- 課金管理は RevenueCat。エンタイトルメント（サポーター/最近コーヒー）→ 広告オフに直結。
- 価格は **¥150/月** を基準。AI原価は **約¥20/世帯・月**（Haiku中心。プロンプトキャッシュは現行プロンプト長＜4,096トークンでは発火しないため、コストは入力でなく**出力トークンと生成回数**が支配的。効かせるなら system に few-shot/詳細ルールを足してプレフィックスを伸ばす）。
- **広告ゼロでサブスクだけ全ユーザーのAIを賄う分岐**（実入り＝登録率×価格×0.85 ≥ ¥20）：登録10%なら **約¥235/月**。¥150は約64%、¥200は約85%、登録12%あれば¥200で到達。
- ただし設計は**無料90%のAIを広告で相殺**する前提なので、その場合は **¥150でも可**（サブスクは「自分のAI＋固定費＋利益」を賄えれば足りる）。広告を最小化したいなら¥200〜235、広告で無料層を支えるなら¥150。
- 黒字化の最低ラインは固定費＝月約¥1,250（Apple年会費）で、サブスク約12人 or 月コーヒー5杯で到達。

---

## 11. MVP範囲 / 将来拡張

### MVP（2タップ献立の成立に集中）
疲労度選択／決めて→AI提案（1〜3品・別の案）／冷蔵庫の手入力＋確認画面／まな板／方向性タグ／食べる人設定＋安全フィルタ／食育履歴＋7日バランス／栄養優先／フォールバック30品／コーヒー単発IAP／ネイティブ広告＋疲労度ゲート。

### MVPで省く
レシートOCR・冷蔵庫写真認識（精度検証後フェーズ2）／家族共有（プレミアム・フェーズ2）／期限プッシュ通知／AI料理画像生成。

### 将来拡張
- フェーズ2：OCR・写真認識・期限通知・家族共有（共有冷蔵庫・担当同期・買い物中在庫確認）。
- フェーズ3：買い物リスト自動生成／特売・ネットスーパー連携／作り置きモード／子の完食学習／音声操作／配偶者への提案共有。

---

## 12. 技術構成

| レイヤー | 採用 |
|---|---|
| クライアント | React Native + Expo（iPhone/iPad両対応。ネイティブ志向ならSwiftUI可） |
| 状態管理 | Zustand 等の軽量 |
| バックエンド | Supabase（Auth/DB/Realtime/Storage）。家族共有の同期に最適 |
| DB | PostgreSQL（Supabase内） |
| AI提案 | Claude API（`claude-haiku-4-5`／構造化出力／プロンプトキャッシュ） |
| OCR | Apple VisionKit（端末内・無料） |
| 画像認識 | オンデバイス CoreML / Vision（フェーズ2） |
| 課金 | RevenueCat |
| 広告 | AdMob（ネイティブ広告のみ・非パーソナライズ前提・ATT最小） |
| 通知 | Expo Push / APNs（フェーズ2） |

オフライン耐性：在庫・履歴はローカルキャッシュ。提案だけ通信、失敗時はフォールバック。

---

## 13. iPhone / iPad 最適化

### iPhone（縦・片手）
- 「決めて」は下部 Thumb Zone に全幅固定。疲労度カードは指1本で押せる大きさ。
- 1画面1タスク。横スクロールは冷蔵庫のみ。Dynamic Type対応。

### iPad（横・キッチン設置）
- 2ペイン（左：冷蔵庫＋まな板／右：提案結果を常時表示＝決定確認用）。
- 遠目視認モード。Split View で買い物リスト並置（フェーズ2）。横向きでもコア動線を維持。

### 共通
- ハプティクス（決定時の達成感）／暖色トーン／余白多め。18〜20時想定の暖色ナイトモード。

---

## 14. 設計上の不変条件（実装時に守る）

1. 意思決定フローに広告・割り込みを入れない。
2. 安全フィルタ（アレルゲン・妊娠中・乳幼児）は候補が減っても**絶対に緩めない**。
3. 「決めて」を押して何も出ない状態を作らない（必ずフォールバック）。
4. 「別の案」は最大3回で打ち切る。
5. 食べる人は毎日全員オンにリセット（前日の外しを引きずらない）。
6. safetyProfile / ingredientDict は AI・検算・フォールバックで共有する単一の真実。
7. 重度アレルギー時は材料確認の注意を常時表示（保証しない旨を明示）。
8. 決定後は当日「今日はこれ」を保持し、再起動で疲労度選択に戻さない（同日再決定は履歴を上書き）。
9. 在庫消費の確認は限界モードでは出さない（疲れた日に作業を足さない）。
